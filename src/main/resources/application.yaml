camunda:
  client:
    mode: self-managed
    zeebe:
      enabled: true
      grpc-address: http://127.0.0.1:26500
      rest-address: http://127.0.0.1:8080

spring.datasource:
  url: jdbc:mysql://localhost:3306/bips_wise24?useSSL=false
  username: root
  password: root